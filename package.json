{
  "name": "open-socmed",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "vercel dev --yes",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "dotenv-cli": "^9.0.0",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
