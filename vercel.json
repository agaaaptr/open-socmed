{
  "version": 2,
  "builds": [
    {
      "src": "apps/backend/api/**/*.go",
      "use": "@vercel/go"
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}