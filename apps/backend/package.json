{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e .env.local -- go run main.go",
    "build": "go build -o dist/main main.go"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
