{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e .env.local -- go run main.go",
    "build": "echo 'Backend build handled by Vercel functions'"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
