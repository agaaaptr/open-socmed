{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Backend development handled by vercel dev from root'",
    "build": "echo 'Backend build handled by Vercel functions'"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
